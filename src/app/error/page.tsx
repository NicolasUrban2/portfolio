'use client';

export default function ErrorPage() {
    // TODO: add contact so users can report the error
    return (
        <div className="text-center">
            <h1>Error</h1>
            <p>Something went wrong.</p>
        </div>
    );
}